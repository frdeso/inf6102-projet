#include "agent.h"
#include <assert.h>  
agent::agent(){
	assert(false);
}

agent::agent(agent &a){
	assert(false);
}

direction agent::chooseDirection(grid &g){
	assert(false);
	return direction::NORTH;
}